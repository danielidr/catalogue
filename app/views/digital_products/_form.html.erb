<%= form_with(model: digital_product, local: true) do |form| %>
  <% if digital_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(digital_product.errors.count, "error") %> prohibited this digital_product from being saved:</h2>

      <ul>
      <% digital_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= form.label :product %>
      <%= form.select :product_id, options_for_select(@products), prompt: "Select product" %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
