<%= form_with(model: physical_product, local: true) do |form| %>
  <% if physical_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physical_product.errors.count, "error") %> prohibited this physical_product from being saved:</h2>

      <ul>
      <% physical_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= form.label :product %>
      <%= form.select :product_id, options_for_select(@products), prompt: "Select product" %>
  </div>
  
  <div class="field">
    <%= form.label :images %>
    <%= form.file_field :images, multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
